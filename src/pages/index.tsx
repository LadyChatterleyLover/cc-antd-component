import styles from './index.less'

const Index = () => {
  return (
    <div className={styles.container}>
      <div className={styles.title}>antd + typescript 二次封装组件</div>
    </div>
  )
}

export default Index
